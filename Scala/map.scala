// using the map function
// we can manipulate the function and return new list with the outputs

val numbers = List(1,2,3,4,5)

val doubleNumbers = numbers.map((number) => number*2)

println(numbers)
println(doubleNumbers)

// this is awesome