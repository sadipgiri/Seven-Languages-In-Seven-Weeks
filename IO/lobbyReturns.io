//What does Lobby return?

"Let's see what lobbby returns when we type Lobby in Io language." println

Lobby

"Lobby returns some kinds of object_id, Protos_id, and other objects(variables, methods..) that we initialized." println

"Therefore, I think Lobby is some kind of global namespace in Io language." println
